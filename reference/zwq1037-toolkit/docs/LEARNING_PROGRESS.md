# 学习进度与课程交接

这是本项目课程状态的唯一事实来源。开始新课前必须结合 `README.md`、`PROJECT_PLAN.md`、`TEACHING_WORKFLOW.md` 和 `LEARNING_RESOURCES.md` 阅读本文件。

## 当前阶段

阶段 6：1D CNN 时间序列模型。

说明：阶段0～5的核心实战流程已验收，MLP全量cosine为0.0446837080，LightGBM为0.1038700392。已核对数据对齐与保存模型恢复预测一致性；不以MLP必须胜过LightGBM作为进入CNN的前提。模型加载接口尚需在后续实际使用时向学习者补讲。

## 已验收课程

| 课程 | 状态 | 已掌握内容 |
|---|---|---|
| 第 0 课 | 已验收 | Conda 环境、依赖、Feather 与资源限制 |
| 第 1A 课 | 已验收 | 数据地图、Kaggle 认证与文件规模 |
| 第 1B 课 | 已验收 | 读取标签表、shape、列类型、缺失值和 target |
| 第 2A 课 | 已验收 | 点积、向量长度、余弦相似度、缩放和零向量 |
| 第 2B 课 | 已验收 | 布尔掩码、按月份向前切分、时间泄漏与 `.copy()` 的使用边界 |
| 第 3A 课 | 已验收 | 内存映射、Arrow schema、IPC File/Stream、RecordBatch 与相对预测时间 |
| 第 3B 课 | 已验收 | 按 `sample_id` 分组计数、按倒计时排序，以及一段 market 序列对应一个 target |
| 第 3C 课 | 已验收 | 组内 `.diff()`、时间空档、逐行布尔验证，以及有含义的缺失 |
| 第 3D 课 | 已验收 | 缺失指示列、事实与推断的边界，以及树模型/神经网络的不同缺失处理 |
| 第 4A 课 | 已验收 | 命名聚合 `groupby().agg()`、`reset_index()`，以及一段序列变成一行表格特征 |
| 第 4B 课 | 已验收 | 完整特征与 label 一对一 merge、禁止列检查，以及真实特征表的月份前向划分 |
| 第 4C 课 | 已验收 | LightGBM 回归时间验证、回归连续预测、训练 objective 与早停 metric 的区别，以及保存模型和验证预测 |
| 第 4D 课 | 已验收 | gain 与 split importance、重要性的解释边界，以及从多个候选方案中选择近期窗口实验 |
| 第 4E-A 课 | 已验收 | 根据倒计时筛选最后60秒、布尔掩码、窗口聚合，以及时间窗口不会增加最终样本行数 |
| 第 4E-B 课 | 已验收 | 完整窗口特征构建、空窗口样本保留、公平单变量对照，以及窗口结果的解释边界 |
| 第 4F 课 | 已验收 | 加法模型、L2梯度与二阶梯度、最优叶子输出、叶子收益、split gain、正则化、histogram与leaf-wise |
| 第 5A 课 | 已验收 | Tensor、二维回归输入、shape、float32、CPU/CUDA设备选择，以及to(device)返回值 |
| 第 5B 课 | 已验收 | TensorDataset按行配对、DataLoader分batch、迭代解包、训练集内部shuffle边界，以及逐batch移动设备 |
| 第 5C 课 | 已验收 | 单隐藏层MLP、仿射变换、权重与偏置、ReLU非线性、前向传播，以及batch维度下的shape链条 |
| 第 5D 课 | 已验收 | 训练期中位数填充、训练期均值/标准差、StandardScaler、验证集只transform，以及float32 Tensor |
| 第 5E 课 | 已验收 | nn.Linear、nn.ReLU、nn.Sequential、模型设备、model(batch)前向传播，以及PyTorch参数保存shape |
| 第 5F 课 | 已验收 | MSELoss、0维标量loss、计算图、loss.item()、backward、parameter.grad，以及梯度与更新的区别 |
| 第 5G 课 | 已验收 | SGD、学习率、参数引用、zero_grad与step、参数快照，以及更新后重新前向计算loss |
| 第 5H 课 | 已验收 | epoch/batch/step、双层训练循环、跨轮保留参数、每轮统计重置，以及尾批按实际样本数加权MSE |
| 第 5I/J 合并课 | 已验收 | 真实62维MLP、Adam调用、eval/no_grad、全体验证cosine、最佳参数保存与同时间划分对照 |
| 第 6A 课 | 已验收 | 一维卷积核、局部连接与权重共享、输入/输出通道、响应序列、permute换轴，以及Conv1d前向shape |
| 第 6B 课 | 已验收 | 真实序列按倒计时排序、最近200条截断、短样本左padding/右对齐、14通道、row_mask与has_transactions语义，以及训练期标准化边界 |
| 第 6C-A 检查点 | 已验收 | float16缓存容量估算、`.npy`磁盘映射、分块写入、flush、只读重开，以及存储精度与训练精度的区别 |
| 第 6C-B 检查点 | 已验收 | 单列子进程、manifest断点续建、全量`(N,C,T)`磁盘缓存、训练期统计边界，以及分块完整性验证 |
| 第 6C-C1 检查点 | 已验收 | 保留mask通道轴、广播、沿时间轴求和、有效位置计数、masked mean，以及padding invariance验证 |
| 第 6C-C2 检查点 | 已验收 | 自定义Dataset、position到sample ID映射、memmap单样本float32复制、标签shape，以及真实CNN smoke训练闭环 |
| 第 6C-C3 检查点 | 已验收 | 连续块边界、块级随机排列、`yield`批索引、跨epoch可复现变化，以及训练/验证时间边界 |
| 第 6C 正式实验 | 已验收 | 全量mask-aware CNN训练、最佳权重保存恢复、与MLP/LightGBM公平比较、总体与逐月cosine，以及无状态CNN不具备跨batch记忆 |
| EXP-CNN-003 | 已验收 | 近期月份加权CNN训练20轮、10轮上限诊断、总体与逐月复算，以及CUDA非严格确定性的结论边界 |

## 当前课程

阶段7 / Transformer Encoder：已布置7A单头缩放点积自注意力。

- 状态：EXP-CNN-003已验收并归档。已按课程模式布置7A，等待学习者完成三个核心TODO与两道复盘题。
- 当前CNN最佳：EXP-CNN-003总体cosine为0.1089927233，11个验证月份全部为正，逐月宏观平均0.0896718591；相对当前最佳树模型EXP-TREE-005高0.0028104088。
- 解释边界：本次运行明确显示10轮之后仍有改进，但与EXP-CNN-002的前10轮轨迹不同，不能把两次运行间全部差值纯归因于epoch数量；最佳仍在epoch 20边界。
- 当前任务：学习者阅读指定页并完成`exercises/lesson_07a_single_head_self_attention.py`；Codex随后只读取、运行和解释，不预先代写TODO。
- 边界：Transformer继续使用同一market-only序列缓存、month 0～59训练和60～70验证；7A只演示样本内部位置关系，不包含跨batch记忆、padding mask、位置编码或正式训练。

## 下一候选课

阶段7B / 可学习的多头自注意力与padding mask。

前置条件：7A三个TODO、数值检查与两道复盘题验收通过。7B才使用`nn.MultiheadAttention`，仍不直接启动全量Transformer训练；位置编码留到7C。

原独立5K取消：恢复一致性检查与基线复盘已经在5I/J全量诊断中执行；加载接口留待后续按需讲解，不据此声称学习者已亲手掌握恢复代码。

## 最近一次交接

- EXP-CNN-003已由学习者全量运行并经Codex独立验收：运行目录data/interim/exp_cnn_003_recency_weighted_20epoch_runs/full_20260909_173548，20轮最佳为epoch 20，cosine=0.1089927233，恢复权重一致；11个月全部为正，逐月宏观平均0.0896718591，相对当前最佳树模型EXP-TREE-005高0.0028104088。配置和预测对齐无误。两次同种子CUDA运行的前10轮轨迹不一致，因此不能把相对EXP-CNN-002的全部+0.047235视为纯epoch因果效应；但本次运行中10轮之后多次明显刷新，支持原10轮上限过短。下一步按学习者要求恢复课程模式进入Transformer。
- EXP-CNN-002已由Codex直接实现并完成全量10轮：总体cosine为0.0617581353，相对EXP-CNN-001只提高0.0000303896；但逐月配对显示11个月中9个月改善，宏观逐月均值0.0398140730→0.0563961529，负分月份3→1。产物位于data/interim/lesson_06d_recency_weighted_cnn_runs/full_20260909_163406，实验记录为experiments/EXP_CNN_002_RECENCY_WEIGHTED_MSE.md。学习者注意到连续两项CNN实验最佳轮都在epoch 10边界，决定下一步只把训练上限增加到20轮；由Codex准备脚本，学习者自行运行，进入Transformer时恢复课程模式。
- 学习者完成EXP-CNN-001结果判断：能依据CNN高于MLP但低于LightGBM选择A/B、排除只看训练MSE的C，并理解普通前馈CNN没有跨batch状态；第6C正式实验已验收。学习者选择下一步先试近期月份加权MSE，并明确要求本项CNN实验由Codex直接完成，不采用课程模式；进入Transformer时恢复课程模式。
- 学习者主动指出EXP-CNN-001没有建模月份顺序。按月复算验证预测确认明显不稳定：month 60/63/69 cosine分别为`-0.024215/-0.018039/-0.039149`，month 66/70为`0.116373/0.123683`。当前模型只学习样本内部200位置序列；month只用于时间边界，普通前馈CNN不会因DataLoader按月排列而获得跨batch记忆。诊断已补入实验记录；下一步优先候选为按月稳定性与时间漂移审计，而非直接加深CNN。
- EXP-CNN-001全量运行已核验：运行目录`data/interim/lesson_06c_full_cnn_runs/full_20260909_133512`记录CUDA、训练1,064,163行、验证193,474行、month 0～59/60～70、batch 512和10轮。最佳为epoch 10，cosine=`0.0617277457`，恢复权重后相同；相对MLP `+0.0170440377`，相对LightGBM `-0.0421422935`。逐轮cosine明显波动，预测std `0.00016288`远小于target `0.00292967`，模型MSE `8.55519e-6`仅略优于全零预测`8.58457e-6`。结果已写入`experiments/EXP_CNN_001_MARKET_SEQUENCE.md`和`experiments.csv`；等待学习者完成A/B/C证据判断，不据最后一轮最高直接断言继续训练必然提高。
- EXP-CNN-001固定脚本教师smoke通过：CUDA上20,000训练/5,000验证、40/10 batches，单样本`(14,200)/(1,)`、首批`(512,14,200)/(512,1)`；1轮MSE=`0.00038874`、smoke cosine=`-0.004804`、约2.13秒，保存后恢复cosine一致。该分数仅验证管线，不能与完整基线比较。教师沙箱使用`MSCAPITAL_CNN_OUTPUT_ROOT`写入`F:\深度学习\.codex_qa`；学习者正常运行仍默认写项目`data/interim/lesson_06c_full_cnn_runs`。等待学习者亲自运行默认smoke。
- 已进入EXP-CNN-001正式实验：新增完整讲义与可运行脚本，固定复用已验收的memmap Dataset、block shuffle、两层kernel=5 mask-aware CNN、MSE/Adam、时间验证和最佳权重保存；新增实际需要的`torch.load(..., weights_only=True)`与`load_state_dict()`恢复检查。默认先做20,000/5,000样本、1 epoch短检查，随后`--full`使用1,064,163/193,474样本训练10轮。正式结果尚未产生，不能提前与基线比较。
- 6C-C3验收通过，并按学习者明确要求由Codex修正变量作用域：删除从旧练习导入的全局`batch_size`，块边界统一读取当前采样器的`self.batch_size`；同时补全三条复盘。实测两轮分别得到`[[0,1,2,3],[8,9],[4,5,6,7]]`和`[[4,5,6,7],[0,1,2,3],[8,9]]`，每轮位置0～9恰好出现一次、块内位置连续、跨epoch块顺序改变。下一步直接将其接入全量CNN训练，不再增加准备性检查点。
- 6C-C2最终验收通过并按学习者明确要求由Codex修正最后一处：target改为`self.all_targets[sample_id]`并整理缩进，复盘答案补全。运行得到单样本`(14,200)/(1,)`、首批`(512,14,200)/(512,1)`，CUDA三轮MSE`0.00056573→0.00005946→0.00001881`、cosine`0.005793→0.032005→0.037872`，最佳epoch=3；教师验收只临时把输出改至系统Temp以绕过沙箱，学习者文件训练逻辑不变。现布置最后准备C3 block shuffle，之后直接全量训练。
- 已布置并验收6C-C3：讲义解释逐样本随机、完全顺序与块级打乱的I/O权衡，`batch_sampler`、`yield`、`torch.randperm`、块边界及时间泄漏边界；10样本练习验证每轮全覆盖、块内连续、跨轮顺序变化。
- 学习者进一步指出不会TODO的直接原因是练习固定代码缺少变量来源注释。已在C2练习中补齐中英双语代码旁说明：配置常量、构造函数三个参数、`sequence_cache (N,14,200)`、当前子集`sample_indices (M,)`、完整`all_targets (N,)`、position语义、单样本复制、label时间划分、ID数组shape及创建train/valid Dataset时的参数流向；也补注masked CNN固定代码。4个TODO及学习者已写的TODO1均未改动。
- 学习者反馈6C-C2原讲义不足以把4个Dataset TODO连起来，并曾尝试让`__len__`返回完整`all_targets`长度。已补充40.1贯穿示例：用当前Dataset只含sample ID `[10,25,80]`解释长度必须取`sample_indices`、position→sample ID映射、只复制对应memmap切片，以及按同一ID取得shape `(1,)`标签；强调错用完整长度会越界，错配ID会让特征与答案错位。练习文件未修改，等待学习者自行完成。
- 6C-C2教师参考smoke最终通过：内存临时填入4个Dataset TODO，单样本`(14,200)/(1,)`、首批`(512,14,200)/(512,1)`正确，CUDA三轮训练MSE为`0.00056573→0.00005946→0.00001881`，验证cosine为`0.005793→0.032005→0.037871`，每轮约1.21/0.71/0.71秒，最佳epoch=3。教师沙箱不能向项目中文路径写PyTorch权重，故参考产物改存系统临时目录；用户既有环境此前可正常在项目保存。练习TODO未写回。smoke改用时间范围开头连续ID并顺序读取，分数不与完整基线比较。
- 6C-C2教师首次参考smoke在进入训练前发现固定管线性能问题：从全缓存随机抽取ID并逐样本随机读取，GPU利用率为0且数分钟未完成首轮；已终止该教师进程，未生成权重或历史。现把smoke改为训练/验证时间范围开头的连续20000/5000 ID并保持`shuffle=False`，只用于验证闭环；正式训练前另行设计块级shuffle，避免把慢随机I/O交给学习者。
- 已布置6C-C2：讲义新增自定义`Dataset`、`__len__`/`__getitem__`、Dataset position与真实sample ID、单样本`np.array(dtype=float32, copy=True)`及`num_workers=0`边界。练习只留4个Dataset核心TODO；固定代码复用masked CNN和训练验证保存流程，默认20,000训练/5,000验证、3 epoch smoke。尚未教师参考运行，学习者文件未修改。
- 6C-C1验收通过：学习者沿`dim=2`计算masked sums与有效计数，使用`[:,13:14,:]`保留mask通道轴；数字例子普通平均`[[51.5,31.0]]`、masked mean`[[3.0,12.0]]`，padding invariance为True，正式缓存4样本前向`(4,14,200)→(4,1)`且预测有限。`.clamp(min=1)`与本处`.clamp_min(1.0)`等价，不要求形式性修改。下一候选C2磁盘Dataset与训练，尚未布置。
- 已布置6C-C1：只新增保留轴切片与masked mean pooling两个紧密相关概念；练习提供CNN和真实缓存读取骨架，学习者完成masked sum、有效计数、除法及`13:14`取mask。数字测试要求修改padding响应后池化结果不变，随后以4个正式缓存样本检查`(4,14,200)→(4,1)`有限前向。通过后直接进入磁盘Dataset与训练，不另设重复CNN玩具课。
- 6C-B正式缓存已验收：构建于10:45:50结束，manifest为`status=complete`且14通道齐全；只读分块扫描验证`numpy.memmap (1257637,14,200) float16`、文件6.5591GiB、全体有限、mask全为二值、每个样本有效行数均匹配`min(raw_count,200)`、所有padding位置13个非mask通道均为0。12份统计文件均记录`training_sample_count=1064163`，即只使用月份0～59。新增可复用只读检查`tests/validate_train_sequence_cache.py`。下一候选6C-C mask-aware CNN，尚未布置。
- 学习者6C-B smoke验收通过：报告`status=complete`、`(1000,14,200) float16`、抽查计数`[199,107,198]`，第二次运行出现多条completed/reusing记录；已纠正预处理边界，明确验证数据应使用训练期mean/std变换，但不能参与统计量拟合。正式目录尚不存在，F盘约185.7GiB可用；构建脚本已增加缓存本体外2GiB的磁盘安全余量检查。下一步由学习者显式传入`--full`启动正式构建。
- 6C-B固定构建脚本教师smoke验证通过：首次以1000样本/182798条原始行为输入，逐列完成14通道，layout与countdown审计通过，输出`(1000,14,200) float16`且抽查observed counts为`[199,107,198]`；第二次运行正确复用全部已完成通道。前5个row_mask与6B一致；连续特征因smoke与6B拟合统计量的样本范围不同而不要求逐值相同。教师测试目录随后已清理，留给学习者从空目录观察首次构建；正式缓存尚未创建。
- 6C-A验收通过：学习者正确估算float16全量缓存约6.56GiB，使用`open_memmap(mode="w+")`把5个真实`(N,C,T)`序列按`[0:3]`与`[3:5]`连续写入，并以`np.load(..., mmap_mode="r")`只读重开；缓存为`numpy.memmap (5,14,200) float16`，row_mask精确保持，最大float16转换误差0.00313473。能区分内存/磁盘占用、覆盖风险和缓存/训练精度。现进入6C-B，固定脚本先做1000样本smoke与断点复用检查。
- 6B最终验收通过：学习者代码运行得到`(5,200,14)`及CNN布局`(5,14,200)`，观测数`[199,107,133,199,200]`、左padding数`[1,93,67,1,0]`与边界mask均正确；已修正真实无成交行为`row_mask=1, has_transactions=0`，能选择仅以训练期真实行拟合统计量。学习者文件未被Codex修改。现进入6C-A，在5个真实样本上学习float16 `.npy`磁盘映射、分块写入与只读重开；全量构建和训练暂未开始。
- 学习者反馈6B讲义看不明白，并明确希望保留原范围、增加人话细节而非缩小课程。原缩小范围的补丁未应用；现保留对齐、padding、两种mask、14通道、标准化和资源边界，在讲义中新增107行样本贯穿示例、batch为何需统一shape、三种状态表、14通道单行示例、padding污染统计量的数字例子、验证泄漏例子及逐TODO解释。练习范围与TODO保持不变，仍等待学习者完成。
- 已进入6B并核对完整聚合表：1,257,637个样本的market行数范围1～212，中位数189，99%分位201；若直接物化float32 `(1257637,14,200)`约13.1GiB，16GB内存不可一次常驻。小样本证实严格`round(seconds/3)`会发生槽位碰撞，因此第一版采用按倒计时降序、最多保留最近200条、短样本左padding/右对齐，并保留倒计时、has_transactions与row_mask。讲义和三组核心TODO已布置，未修改学习者6A文件，尚未验收6B。
- 6A学习者验收通过：手算输出`[2,3,3]`；`permute(0,2,1)`将`(2,8,3)`正确变为`(2,3,8)`，首样本首通道保持原第一特征的8个时间值；Conv1d权重与中间shape依次为`(8,3,3)`、`(2,8,8)`、`(2,8,1)`、`(2,8)`，最终预测`(2,1)`且CUDA数值有限。复盘方向正确，并补充精确措辞：8个输出通道是8套卷积核产生的8条内部响应序列，不是8个最终预测；把62个聚合数排列成长度62只会让模型在异质统计特征轴上滑动，不能恢复聚合时丢失的时间信息。学习者文件未被Codex修改。下一候选6B。
- 学习者同意进入CNN，已准备6A讲义与三项核心任务，并同步路线/资源安排。尚未验收6A，未修改5I/J学习者代码及实验产物。阅读页码已逐页核对本地D2L；本课强调局部连接、共享权重与(B,C,T)，真实不规则序列处理留到6B。
- 6A教师参考测试通过：仅在内存中填入参考答案，学习者文件保留三个TODO；检查了换轴数值对应、卷积/池化/Flatten各层shape、有限预测、无梯度记录及CPU/CUDA前向。默认CUDA与CPU预测差约1.74e-5，测试进程关闭cuDNN TF32后约2.98e-8，属于计算精度差异，不要求学习者预测逐位一致；未修改环境持久设置，未训练或保存新模型。
- 5I/J全量已验收：学习者产物`outputs/lesson_05ij/full_192bwyml/`，20轮、训练1064163行/验证193474行、62特征，最佳epoch=20，cosine=0.04468370803391311，相对EXP-002绝对变化-0.0591863312。验证ID与target和原标签/LightGBM均一致，完整分数已复算；用保存的预处理器transform原始验证聚合特征，再加载保存权重，重现预测的最大绝对误差约9.31e-10。读取该用户创建目录需要只读提权，未修改权限、代码或模型，未重训。
- 诊断证据：本模型验证MSE=8.603350713e-6，零预测MSE=8.584572128e-6，LightGBM验证MSE=8.493760029e-6；MLP预测均值0.00023343、标准差0.00021953，标签均值0.00003971、标准差0.00292967，Pearson约0.05082（LightGBM约0.10536）。训练loss小不代表预测有效；末轮才刷新最佳，不能仅凭曲线断言过拟合，也未证明单一弱因是学习率或模型容量。不做未经请求的调参。5K中的教师恢复一致性诊断已做过，可复用此证据，不机械重复跑检查。
- 5I/J修正后试跑通过：运行原练习文件（未改代码），CUDA三轮训练MSE为0.013415/0.003339/0.001604，验证cosine为0.033130/0.007713/0.012890，正确保存第1轮而不是末轮。产物`outputs/lesson_05ij/smoke_ddp01vk7/`；检查了history最大值、最佳epoch、5000行预测及ID、CPU重载权重预测一致性、全体cosine复算、scaler仅fit20000行及验证无计算图。下一步保持其余参数不变，仅将SMOKE_TEST=False完成全量20轮；不将试跑分数与全量LightGBM比较，当前不标整课完成。
- 5I/J首次学习者检查：CUDA完成第一轮训练及验证预测后，在第107行`assert np.isfinite(validation_cosine)`报ValueError（多元素数组真值不明确）。根因是第106行仅写`(validation_targets, validation_predictions)`，创建两个数组组成的tuple，未调用`cosine_similarity_score`返回标量；不能通过加all/any掩盖。第116行保存权重仍为省略号。其余验证流程和最佳分数比较方向正确，复盘能区分eval/no_grad及验证泄漏。练习未修改；本次不完整试跑目录为`outputs/lesson_05ij/smoke_lz92fuku/`，不算完成实验。
- 已按学习者请求合并5I/5J：验证概念已在旧项目掌握，不重做玩具验证课。真实特征只逐列读取现成聚合表；核心练习留空，参考实现测试不等于学习者验收。
- 合并课准备验证：独立参考实现完成CUDA三轮真实数据试跑，检查了最佳轮并非末轮时的保存、CPU重载预测一致性、预测ID顺序、训练期scaler样本数及no_grad；产物隔离在`tmp/lesson_05ij_qa/`。另独立验证完整数据预处理，得到正确的1064163/193474行与62列；未运行全量训练，未生成正式MLP实验结论。
- 第5H已验收：学习者自行修正样本累计变量，CUDA重跑20轮均为3批、5个样本，尾批大小2/2/1，总计60次更新；训练途中的加权MSE从第1轮`0.060666`降到第20轮`0.000285`。总step回答改为60，并能指出MSE与cosine不可直接比较；已补充训练下降不证明验证提升的边界。首次错误仅影响日志分母，不影响参数更新，现已解决。练习未被Codex修改；下一候选课为5I。
- 第 2A 课最终运行结果约为 `1.0、0.8、-1.0、1.0、0.0`，已通过。
- 第 2B 课正确得到训练月份 `0～59`、验证月份 `60～70`，两部分共 `1,257,637` 行；代码与概念复盘均已通过。
- 学习者理解随机切分会造成未来信息泄漏，也理解 `.copy()` 的独立性与内存成本之间需要权衡。
- 八个官方文件已全部下载到 `data/raw`，合计约 9.27 GiB，实际字节数均与 Kaggle 官方清单一致。
- 第 3A 课已确认 market 有 13 列，关键列包括 `sample_id`、`seconds_before_predict`、成交特征和两档买卖盘特征；文件只有 1 个 RecordBatch，因此没有读取该巨大批次。
- 第 3A 代码和概念复盘均已通过；学习者能解释 schema 不等于完整数据、唯一 RecordBatch 的内存风险，以及相对预测时间。
- 已通过逐列、短进程方式安全导出样本 ID 0～4：market 为 838 行、13 列，label 为 5 行、3 列；没有把 13 个完整列同时放入内存。
- 5 个样本分别包含约 100～200 个时间步；`transaction_avgprice` 存在缺失，其他 12 列在该小样本中没有缺失。其原因和处理方式留到第 3C 课验证。
- 第 3B 课已通过：学习者能使用 `groupby(...).size()`、布尔条件和 `sort_values()`，并理解倒计时数值越小越接近预测时点。曾出现方法名与左圆括号分行导致的语法错误，已自行修正。
- 第 3C 课已通过：学习者能解释长时间空档会减少记录行数，零成交导致平均成交价无法计算，删除这类行会丢失市场状态；也能区分原始字段缺失与 `.diff()` 在每组第一行自然产生的 `NaN`。
- 跨项目能力已复核：上一项目已完成决策树、随机森林、Gradient Boosting、LightGBM、early stopping、OOF 和公平模型对照；本项目阶段 4 不再把这些内容当成新课。
- 第 3D 课已通过：学习者理解新增指示列不改变样本行数，布尔指示不能替代成交次数；LightGBM 可先保留缺失，神经网络需用安全值避免 `NaN` 在 loss/梯度中传播，同时保留 mask 表达“没有成交”。
- 第 4A 课已通过：学习者理解分组聚合会保留总量、比例和平均水平，但丢失逐时刻细节与顺序；亲手完成分组、两个命名聚合示例和 `reset_index()`，其余 10 个重复聚合项按请求由 Codex 补齐。
- 第 4B 课已通过：学习者理解按位置拼接可能错配，`one_to_one` 会阻止任一侧重复键；也理解 month 主要用于 forward validation，树模型虽能按月份切分，但不能自然外推到训练范围外，因此第一版先测 market-only 特征。
- 第 4C 课已通过：LightGBM 使用L2回归目标训练树，以自定义余弦指标选择最佳轮数；最佳轮数356，本地月份60～70的余弦分数约0.077821。学习者理解回归预测通常是形状 `(样本数,)` 的一维数组，每个样本只有一个连续预测值，因此不需要分类概率中的 `[:, 1]`；也能区分本地验证集与月份71～108的Kaggle测试集。
- 第4D课已通过：特征重要性表为 `(40, 4)`，gain占比合计100%；前三名为 `ask_price_1_last`、`bid_price_1_last`、`spread_1_mean`。学习者能区分split使用次数与gain累计训练收益，识别不经验证直接删除低重要性特征的推理问题，并自主选择最后60秒窗口作为下一实验方向。
- 第4E-A课已通过：从838行小样本中正确筛选88行最后60秒记录，再按5个sample_id聚合成5行窗口表；学习者理解倒计时10秒比120秒更接近预测时点，也理解窗口原始行最终会压缩为每个样本一行。运行前合理预测窗口特征可能因与V1重复而不提升。
- 第4E-B课已通过：正式公平对照为0.103870，相比EXP-001的0.077821绝对提高0.026049；学习者正确解释结果支持近期窗口有效，但不能证明60秒是最佳窗口。验收发现并修复了inner join误删5个空窗口训练样本的问题；修正版保留全部1,257,637个sample_id。
- 第4F课已通过：学习者能手算逐样本梯度、叶子G/H、最优叶子输出、split gain、学习率更新及L2损失变化，并理解把最优w代回二阶近似损失可得到叶子收益。措辞复盘明确：early stopping监控的是验证指标连续若干轮未刷新最佳值，而不是要求指标完全不变化。
- 第5A课已通过：练习得到features shape `(4, 3)`、targets shape `(4, 1)`，两者均为`torch.float32`并成功移动到`cuda:0`；学习者能解释Tensor与DataFrame的职责差异、to(device)返回值以及模型和数据必须位于同一设备。
- 第5B课已通过：5个样本以batch size 2完整产生3批，features shape依次为`(2,3)、(2,3)、(1,3)`，targets依次为`(2,1)、(2,1)、(1,1)`，所有batch成功移动到`cuda:0`；学习者理解TensorDataset保持配对，训练集内部shuffle不会混入已分离的验证月份。
- 第5C课已通过：学习者正确手算`z=(-1,3)`、ReLU后`h=(0,3)`与输出`-2.5`，并正确写出`(4,3)→(4,2)→(4,1)`的shape链条；能区分一个含两个神经元的隐藏层与两个隐藏层，也理解隐藏神经元并非人预先指定的金融概念。
- 第5D课已通过：首次运行误将含NaN的原始数组传给StandardScaler，学习者自行修正为填充后的train/valid数组；最终训练中位数`[3,250]`、均值`[3,250]`、scale约`[1.4142,111.8034]`，处理结果无NaN且验证集只transform。
- 第5E课已通过：MLP结构为`Linear(3,4)→ReLU→Linear(4,1)`并在CUDA完成三个batch前向传播；学习者修正了输出shape与内部weight方向，理解数学$XW$中的`(3,4)`对应PyTorch保存的`(4,3)`，未经训练的随机预测不能评价效果。
- 第5F课已通过：成功用MSELoss得到0维loss并执行backward，所有参数grad与参数shape相同；学习者修正了标量shape、backward前grad为None和PyTorch weight方向，能解释反向传播是沿计算图用链式法则求loss对全部参数的梯度，且backward不会更新权重。
- 第5G课已通过：CUDA实测单batch MSE从`0.0671458319`降至`0.0429385081`；首个权重从`0.4414064884`变为`0.4401803017`，与SGD公式预期`0.4401803075`在浮点误差内一致。四项运行前预测及参数引用判断均正确；step后梯度仍存在。无阻塞问题；非阻塞建议为优化器使用`lr=learning_rate`，避免以后调参时与验算学习率不一致。未修改学习者练习文件；下一候选课为5H完整epoch与加权训练loss。
