# Joint-GRU 双种子与嵌入树全量候选

## 训练设置

- 训练月份：0–70，共 1,257,637 行。
- 测试集：647,896 行。
- Joint-GRU：seed 42 与 137，各训练 6 轮；319 个静态特征加完整 200 步序列。
- 嵌入树：319 个静态特征加两个种子平均后的 96 维 GRU 表征，共 415 维；XGBoost 800 棵、深度 5。
- 最终有效权重：67.5% Relative319 TabM trim1、18% XGB053R、4.5% GRU 嵌入树、10% 双种子 Joint-GRU。

两颗 Joint-GRU 的最终训练 MSE 分别为 0.958713 和 0.958965，训练轨迹接近，没有发现种子异常。嵌入树训练耗时约 98 秒。

## 文件验收

- 候选文件包含 647,896 行，字段为 `sample_id,prediction`。
- 样本顺序与当前 Public 0.134 候选完全一致。
- 所有预测均为有限值，没有缺失值。
- 与当前 Public 0.134 文件的余弦相似度为 0.998763，相对 L2 差异为 0.055651。
- 这是有意义但较保守的改动，符合验证阶段只给新 GRU 信号小权重的结论。

候选文件：`outputs/submissions/gru_embedding_seed_ensemble_fulltrain.csv`。状态为已准备、未上传；只有 Kaggle Public LB 才能回答是否真正超过 0.134。

